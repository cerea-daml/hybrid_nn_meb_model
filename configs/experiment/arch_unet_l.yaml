# @package _global_

defaults:
  - override /data: offline_module
  - override /network: offline_deterministic
  - override /network/backbone: unet

exp_name: unet_large

network:
  backbone:
    n_depth: 3

logger:
  tags:
    - architecture
    - unet
